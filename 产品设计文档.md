这是一份针对“基于 Agent 的新一代高级计划与排程（APS）系统”的完整产品设计文档（PRD）草案。文档融合了标准软件工程的产品架构，以及针对底层运筹优化算法边界的特殊设计。

# **产品设计文档 (PRD)：基于 Agent 的认知型 APS 系统**

## **1\. 产品概述**

### **1.1 产品愿景**

打造一款“听得懂业务语言”的工业级排程系统。通过大语言模型（LLM Agent）与底层运筹优化引擎（OR Engine）的深度融合，解决传统 APS 系统“规则僵化、调参门槛高、难以应对现场临时变动”的行业痛点。

### **1.2 核心价值**

* **降本增效**：业务人员无需运筹学背景，通过自然语言即可下达特殊排产指令。  
* **模型稳定**：系统底座采用“微内核+插件化”设计，剥离非标数据，保障核心求解算法的数学严谨性和收敛性。  
* **极速响应**：通过标准化的 JSON 约束协议，实现业务意图到数学模型的毫秒级动态注入。

## **2\. 系统架构设计**

系统采用前后端分离与模块化架构，核心分为四层：

### **2.1 交互展示层 (Web UI)**

* **技术栈**：Vue 3 框架。  
* **核心组件**：  
  * **交互式甘特图**：展示任务在各资源节点（如连铸机、炉台）上的排布，支持人工拖拽微调。  
  * **Agent 对话视窗**：悬浮式的自然语言指令输入框，展示 Agent 解析过程与模型反馈。  
  * **KPI 仪表盘**：实时展示当前排程方案的产能利用率、按时交货率、切换惩罚分等核心指标。

### **2.2 认知与解析层 (Agent Core)**

* **职责**：作为系统的“翻译官”，理解业务意图并将其结构化。  
* **核心能力**：  
  * 实体识别：将自然语言映射到标准数据字典（Tasks, Resources, Rules）。  
  * 指令转化：生成符合系统底层规范的 data\_overrides（基础数据覆写）和 rule\_injections（规则动态注入）JSON 指令。

### **2.3 核心求解底座 (OR Engine)**

* **技术栈**：Python 环境，底层兼容 Gurobi、OR-Tools 求解器或 ALNS 启发式算法框架。  
* **设计原则**：**计算逻辑与返回格式的绝对不可变性**。无论 Agent 注入何种约束，算法底层的核心计算方式（如吨位计算、时间推演）和最终的 JSON 返回结构必须保持原样，严禁 Agent 篡改函数的返回结构。

### **2.4 数据与部署层**

* **数据适配器 (Data Adapter)**：负责将外部系统（ERP/MES）杂乱的数据标准化。  
* **部署方案**：全组件容器化，依托 Docker 进行镜像打包与服务编排，确保多项目交付时环境的一致性。

## **3\. 核心功能模块与业务逻辑**

### **3.1 标准数据适配与预处理 (Data Standardization)**

在核心模型实例化之前，必须执行严格的数据重组流水线。

* **配置数据前置合并**：禁止在核心计算逻辑中临时拉取业务参数。必须在数据预处理阶段，就将 order\_data 中携带的个性化配置参数提前抽取并重组，统一并入全局的 config\_data 中。  
* **脏数据清洗**：针对工业现场常见的异常（如组炉过程中的炉容容量误差，或未满月炉台的历史经验值缺失），在适配层统一进行平滑和填充，输出干净的 DTO（数据传输对象）。

### **3.2 动态约束注入系统 (Hook System)**

算法底层预留标准化 API，承接 Agent 翻译的 JSON 协议：

* **硬约束注入 (Hard Hooks)**：执行变量域限制。例如，锁定特定任务必须分配给指定设备（$X\_{ik} \= 1$），或屏蔽特定时段。  
* **软约束/目标注入 (Soft Hooks)**：动态调整目标函数惩罚项的权重。例如，针对宽度过渡、钢种切换等特征，根据 Agent 指令临时放大或缩小权重惩罚。

### **3.3 迭代与评分评估机制**

在使用启发式算法（如 ALNS）进行动态求解时，评分系统的设计必须兼容 Agent 带来的规则扰动：

* **破除贪心解迷信**：迭代过程中，当前解（initial\_score）的评价不应被初始的贪心解（greedy\_score）死死限制。由于 Agent 注入了新的权重，系统在迭代搜索时，initial\_score 很有可能（也应当被允许）在某些维度超过 greedy\_score，评分校验机制必须具备这种宽容度，避免因初次比较失败而错杀优质解。

## **4\. 冲突与异常处理机制 (Conflict Resolution)**

工业现场的需求往往自相矛盾。当 Agent 注入的指令导致底层模型**无解 (Infeasible)** 时，系统采用“三级降级与交互机制”：

### **4.1 第一级：Agent 前置逻辑校验 (Pre-check)**

Agent 在生成 JSON 协议前，需进行基础物理逻辑校验。

* *场景*：客户要求“今天把 5000 吨单子在 1 号炉做完”。  
* *拦截*：Agent 读取标准数据，发现 1 号炉日产能上限仅 3000 吨，直接在对话框提示客户产能不足，拒绝生成硬约束。

### **4.2 第二级：算法底层自动降级 (Auto-Degradation)**

如果指令通过了预检查，但在 Gurobi 或模型求解时引发了约束冲突（Infeasible Model）：

* 算法底座将捕获异常，并触发**约束松弛 (Constraint Relaxation)**。  
* 系统会自动将 Agent 刚刚注入的 **“硬约束 (Hard Rule)” 降级为 “极高权重的软约束 (Soft Objective)”**，重新求解，确保系统永远能输出一个排产计划，而不是崩溃。

### **4.3 第三级：人机协同决策 (Human-in-the-Loop)**

在完成自动降级求解后，UI 端会高亮显示冲突区域，Agent 会主动向用户解释：

* *提示语*：“您要求 A 批次必须在今天完成，且必须使用 2 号炉台。但由于容量限制，这两个条件无法同时满足。系统已为您生成妥协方案（将部分 A 批次延期到明天）。您是否需要调整原始需求？”

## **5\. 项目实施与演进路线图**

### **Phase 1：基建与标准化 (核心底座重构)**

* 完成 Data Adapter 层的研发，实现业务数据到标准 DTO 的转换。  
* 完善底层算法（OR-Tools/ALNS）的 Hook 接口，确保外挂惩罚项和硬约束时不影响原有计算逻辑和返回格式。  
* 确立 Agent 与模型通信的 JSON Schema 规范。

### **Phase 2：Agent 接入与闭环 (单点实验)**

* 引入大模型能力，开发 Prompt 模板库。  
* 在前端 UI 集成对话视窗。  
* 重点跑通“需求输入 \-\> JSON 解析 \-\> 权重调整 \-\> 甘特图重绘”的核心数据流。

### **Phase 3：多场景工程化打样 (规模化交付)**

* 针对具体项目（如处理序列号编码及格式设置等定制化输出需求）进行前端展示层的适配。  
* 完善日志系统，记录 Agent 的每一次决策注入，便于后期溯源和算法调优。  
* 沉淀行业的常见特殊需求，形成 Agent 的“肌肉记忆”，进一步提升意图识别的准确率。